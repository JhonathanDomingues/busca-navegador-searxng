services:
  searxng:
    image: docker.io/searxng/searxng
    container_name: searxng

    ports:
      - "8080:8080"

    volumes:
      - ./searxng:/etc/searxng

    security_opt:
      - label=disable

    restart: unless-stopped